<footer class="footer mt-3">
    <div class="container">
        <div class="text-center">
            <% if buyer_signed_in? %>
            <p class="text-success lead">Buyer Account | Signed in as <%= current_buyer.email.truncate(30) %> <p>
            <% elsif broker_signed_in? %>
            <p class="text-primary lead">Broker Account | Name: <%= current_broker.broker_name %> | Signed in as <%= current_broker.email.truncate(30) %> <p>
            <% elsif admin_signed_in? %>
            <p class="lead">Admin Account | Signed in as <%= current_admin.email %> <p>
            <% else %>
            <p class="lead text-white">© 2021<p>
            <% end %>
        </div>
    </div>
</footer>