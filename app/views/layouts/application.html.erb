<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="Stock App">
    <meta name="author" content="E&M - Devs">
    <title>Stock App</title>

    <%= favicon_link_tag asset_path('') %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"
    />

    <style>
    body {
        font-family: 'Merriweather Sans', sans-serif;
        color: #bc6ff1;
        background-color: #333;
        <% if buyer_signed_in? %>
          background: #000522; 
          background-image: url("https://i.imgur.com/WkEO3EDh.png");
        <% elsif broker_signed_in? %>
          background: #000522; 
          background-image: url("https://i.imgur.com/Wa7XTBQh.png");
        <% elsif admin_signed_in? %>
          background: #000419; 
          background-image: url("https://i.imgur.com/Wa7XTBQh.png");
        <% else %>
          background: #00072b;
          background-image: url("https://i.imgur.com/2IMdpWZh.png");
        <% end %>
        background-blend-mode: overlay; 
        background-attachment: fixed;
        background-position: center;
        background-repeat: repeat;
        background-size: cover;
        }
      </style>
    
  </head>

  <body>
    <%= render "layouts/nav" %>
    <%= render 'layouts/messages'%>
    <div class="cover-container d-flex w-100 h-100 p-1 mx-auto flex-column">
      <%= yield %>
    </div>
    <%= render 'layouts/footer'%>
  </body>

  <script>
  // For Navbar
    $(window).scroll(function () {
      if ($(document).scrollTop() > 50) {
        $(".nav1").addClass("affix");
      } else {
        $(".nav1").removeClass("affix");
      }
    });

    $(".navTrigger").click(function () {
      $(this).toggleClass("active");
      $("#mainListDiv").toggleClass("show_list");
      $("#mainListDiv").fadeIn();
    });

    $(document).ready(function(){
    $("#dropdown").hover(function(){
        var dropdownMenu = $(this).children(".dropdown-menu");
        if(dropdownMenu.is(":visible")){
            dropdownMenu.parent().toggleClass("open");
        }
    });
    });    
  </script>
</html>
